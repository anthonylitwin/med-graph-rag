version: '3.9'

services:
  api:
    build:
      context: ./apps/api
    command: python /app/main.py
  ui:
    build:
      context: ./apps/ui
    command: npm run dev -- --host 0.0.0.0 --port 5173
    ports:
      - "5173:5173"
